🚀 Features
Show all libraries: View a comprehensive list of stored libraries with their code descriptions.

Smart Search: The ability to search for a specific library by name.

Protection system: Verifies user input and prevents the program from crashing when wrong data is entered.

Arabic language support: UTF-8 encoding was used to support Arabic texts in the console interface.
🛠️ Programming concepts used (Technical Highlights)
1. Object-Oriented Programming (OOP)
Classes were used to organize data, where the library object represents a model containing:

Properties: name, description, code.

Verbs: display function display().

2. Dynamic matrices (Vectors)
Using std::vector to store objects, allowing any number of libraries to be added in the future without having to specify a fixed size for the array.
3. Error handling (Input Validation)
Robust logic has been built to handle input errors (such as entering text instead of a number) using cin.clear() and cin.ignore(), with a user-defined attempt system.
🚀 المميزات (Features)
عرض كافة المكتبات: استعراض قائمة شاملة للمكتبات المخزنة مع وصفها البرمجي.

البحث الذكي: إمكانية البحث عن مكتبة محددة بالاسم.

نظام الحماية: التحقق من مدخلات المستخدم ومنع البرنامج من الانهيار عند إدخال بيانات خاطئة.

دعم اللغة العربية: تم استخدام ترميز UTF-8 لدعم النصوص العربية في واجهة التحكم (Console).

🛠️ المفاهيم البرمجية المستخدمة (Technical Highlights)
البرمجة كائنية التوجه (OOP)
تم استخدام الكلاسات (Classes) لتنظيم البيانات، حيث يمثل كائن library نموذجاً يحتوي على:

الخصائص: الاسم، الوصف، الكود البرمجي.

الأفعال: دالة العرض display()

المصفوفات الديناميكية (Vectors)
استخدام std::vector لتخزين الكائنات، مما يسمح بإضافة أي عدد من المكتبات مستقبلاً دون الحاجة لتحديد حجم ثابت للمصفوفة.

معالجة الأخطاء (Input Validation)
تم بناء منطق قوي للتعامل مع أخطاء الإدخال (مثل إدخال نص بدلاً من رقم) باستخدام cin.clear() و cin.ignore()، مع نظام محاولات محدد للمستخدم.
